# G-10 shared-runtime proof - 2026-08-28

Two consumers used `text.generate` and `document.extract_structured` on
loopback. Engine identity changed from `test-v1` to `test-v2` without editing
consumer code. Sillage stored `SYN-0042` in its own SQLite file.

Not done: live Hermes skill install, production Sillage, permanent service,
UI-01, Hermes `config.yaml`.
