[project]
name = "hermes-local-ai-runtime-bootstrap"
version = "0.1.0"
description = "Validation tooling for the Hermes Local AI Runtime bootstrap"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
