# G-09 packaging and safe installation - 2026-08-28

Lot authorised by explicit `GO G-09`. No permanent systemd service, no model
download, no Proxmox mutation, no live UI, no Hermes `config.yaml` change.

## Default

Rootless prefix install, listen `127.0.0.1`, user unit shipped and not
enabled. Model-store quota 30 GiB. Models are registered separately.

## Commands

```text
python3 -m installkit plan --prefix /tmp/hlair --source-root .
python3 -m installkit install --prefix /tmp/hlair --source-root .
python3 -m installkit uninstall --prefix /tmp/hlair --purge
```

## Proof

See `operations/G09-PACKAGING-2026-08-28.md`.

## Not in this lot

G-10 consumers, G-11 public release, enabling the user unit, container
images, signed GitHub releases.
